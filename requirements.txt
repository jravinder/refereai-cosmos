# RefereAI x Cosmos Reason 2
# Core dependencies for running inference
httpx==0.24.0
python-dotenv==1.0.0

# Server (cosmos_server.py) — only needed if serving the model locally
# pip install -r requirements-server.txt
fastapi==0.104.0
uvicorn==0.24.0
torch==2.1.0
transformers==4.40.0
qwen-vl-utils==0.0.2
opencv-python==4.8.0
Pillow==10.0.0
